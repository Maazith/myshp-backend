# Core Django
Django==4.2.10
djangorestframework==3.15.1
django-cors-headers==4.3.1
djangorestframework-simplejwt==5.3.1

# Database
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# Image processing
Pillow==10.4.0           # For image uploads (product images, banners) - Updated for compatibility

# Production server
gunicorn==21.2.0          # For production servers like Render

# Static files
whitenoise==6.6.0         # For serving static files in production

# Environment variables (optional, for local development)
python-dotenv==1.0.1